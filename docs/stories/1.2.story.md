# Story 1.2: Endpoint REST `/webhook/dashboard/revert`

**Status:** Done (consolidado no workflow `AJypFIeC4rMcs18P` com Story 1.1 — ver ADR-030)
**URL produção:** `GET https://n8n.escalanegociosdigitais.com.br/webhook/dashboard/revert`
**Auth:** Header `Authorization: Bearer <DASHBOARD_API_TOKEN>` (64 chars hex, em `.env.local`)
**Cenários validados:** sem token → `{ok:false, error:'INVALID_TOKEN'}` ✅ | token errado → `INVALID_TOKEN` ✅ | token correto → `{ok:true, data: <DashboardCache>, cached_at}` ✅
**CORS:** `Access-Control-Allow-Origin: *` (v1; restringido na Story 1.10)
**Cache:** `workflow.staticData.global.cache` (não usa filesystem; ver ADR-030)
**Epic:** 001 — Dashboard de BI Revert
**Branch:** N/A (workflow no n8n via MCP)
**Estimativa:** 1-2h
**Pré-requisitos:** Story 1.1 DONE + cache JSON em `/tmp/`
**Bloqueia:** 1.3 a 1.9

---

## User Story

Como **frontend Vite do dashboard**, eu preciso de **endpoint REST autenticado** que retorne o JSON consolidado em **chamada única**.

---

## Acceptance Criteria

- [ ] AC1: Workflow `Revert Dashboard API` ativo, separado do Aggregator
- [ ] AC2: Endpoint `GET https://n8n.escalanegociosdigitais.com.br/webhook/dashboard/revert`
- [ ] AC3: Auth Bearer via `Authorization: Bearer <TOKEN>`
- [ ] AC4: Token como Credential no n8n (não em código)
- [ ] AC5: 200 retorna `{ ok: true, data: <DashboardCache> }`
- [ ] AC6: 401 com token ausente/inválido
- [ ] AC7: 503 quando cache não existe (`{ ok: false, error: "CACHE_NOT_READY" }`)
- [ ] AC8: CORS Header `Access-Control-Allow-Origin: *` (v1)
- [ ] AC9: Tempo de resposta < 500ms cache hit
- [ ] AC10: Logs de acesso em `/tmp/dashboard-access.log`
- [ ] AC11: "Available in MCP" habilitado
- [ ] AC12: Export salvo em `docs/n8n-workflows/dashboard-api-v1.json`

---

## Tasks

1. [ ] Criar workflow `Revert Dashboard API` via MCP
2. [ ] Webhook Trigger: GET, path `dashboard/revert`, Response Mode "Last Node"
3. [ ] Gerar token 64 chars: `crypto.randomBytes(32).toString('hex')` ou `openssl rand -hex 32`
4. [ ] Salvar token em `.env.local` como `DASHBOARD_API_TOKEN` + `VITE_DASHBOARD_API_TOKEN`
5. [ ] Code node `Validar Token`:
   - Lê header `Authorization`
   - Compara com Credential
   - 401 se inválido
6. [ ] Read Binary File `/tmp/revert-dashboard-cache.json`
7. [ ] Code node `Montar Response`:
   - Parse JSON
   - Adiciona idade do cache em segundos
   - Monta `{ ok: true, data: <parsed> }`
8. [ ] Respond to Webhook (200):
   - `Content-Type: application/json`
   - `Access-Control-Allow-Origin: *`
   - `Cache-Control: public, max-age=60`
9. [ ] Branch erro cache não pronto (503)
10. [ ] Code node `Log de Acesso` → append em `/tmp/dashboard-access.log`
11. [ ] Testar 3 cenários via curl:
    - Sem header → 401
    - Token errado → 401
    - Token correto → 200
12. [ ] Habilitar "Available in MCP"
13. [ ] Export para `docs/n8n-workflows/dashboard-api-v1.json`

---

## File List

Sem código no repo. Artefatos:
- Workflow `Revert Dashboard API` no n8n
- Token armazenado em `.env.local` + n8n Credentials
- Export: `docs/n8n-workflows/dashboard-api-v1.json`

---

## Notes / Dev Hints

- n8n permite auth nativa por Header no Webhook, mas customização de erro é limitada — validar manualmente via Code dá mais controle
- CORS v1 `*` OK (Bearer protege); v2 restringir ao domínio próprio
- Path completo: `webhook/dashboard/revert` (n8n prefixa)

---

## Definition of Done

- [ ] ACs marcados
- [ ] curl tests passando: 200, 401, 503
- [ ] Token gerado e seguro
- [ ] Export commitado
