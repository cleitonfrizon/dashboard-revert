# Story 1.3: Setup Vite + Auth Shell

**Status:** Done
**Build:** ✅ 218 KB JS (gzip 74 KB, sob alvo de 300 KB) | CSS 13 KB
**Typecheck:** ✅ sem erros
**Lint:** ✅ 0 errors, 1 warning (react-refresh em AuthContext — não bloqueia)
**Dev server:** ✅ `http://localhost:5173/login` HTTP 200
**Bcrypt:** ✅ valida senha contra hash em `VITE_DASHBOARD_PASSWORD_HASH`
**Logo:** SVG fallback (`public/escala-logo.svg`) — TODO trocar pelo PNG oficial (ADR-031)
**Epic:** 001 — Dashboard de BI Revert
**Branch:** `feat/dashboard-revert-shell`
**Estimativa:** 2-3h
**Pré-requisitos:**
- Story 1.2 DONE (endpoint respondendo 200)
- Node 20+ instalado
- Git inicializado na pasta + remote conectado

---

## User Story

Como **Robson, Paulo ou Cleiton**, eu preciso de **login + layout base do dashboard** rodando em Vite/React, para que **eu acesse com segurança e os blocos de dados sejam plugados em seguida**.

---

## Acceptance Criteria

- [ ] AC1: Projeto Vite + React 18 + TypeScript criado na raiz
- [ ] AC2: Tailwind CSS configurado com paleta Escala (ver `docs/brand-system.md`)
- [ ] AC3: shadcn/ui inicializado com tema dark + componentes base
- [ ] AC4: Identidade visual Escala aplicada (`brand-system.md`)
- [ ] AC5: Página de login com senha (bcrypt hash em env)
- [ ] AC6: Após login, redireciona para `/dashboard` com layout shell
- [ ] AC7: Layout shell contém:
   - Header: logo Escala (56px) + "REVERT ENERGIA SOLAR" + última atualização + botão logout
   - PeriodFilter global (hoje, 7d, 30d, mês atual)
   - Grid responsivo com 6 cards placeholder "Em construção"
   - Footer com tagline + cidades + CNPJ
- [ ] AC8: Hook `useDashboardData()` faz fetch do endpoint do n8n com Bearer token
- [ ] AC9: Loading skeleton (shadcn) + error state com "Tentar novamente"
- [ ] AC10: Refresh automático a cada 5 minutos
- [ ] AC11: Tipagem TypeScript completa do `DashboardCache` em `src/lib/types.ts`
- [ ] AC12: Build sem warnings: `npm run build`
- [ ] AC13: Lint passando: `npm run lint`
- [ ] AC14: Typecheck: `npx tsc --noEmit`
- [ ] AC15: Responsivo: desktop 1280px+ e tablet/mobile

---

## Tasks

### Setup do projeto

1. [ ] `npm create vite@latest . -- --template react-ts` (ponto = pasta atual)
2. [ ] `npm install`
3. [ ] Tailwind:
   ```bash
   npm i -D tailwindcss postcss autoprefixer
   npx tailwindcss init -p
   ```
4. [ ] Substituir `tailwind.config.js` por config Escala (de `docs/brand-system.md`)
5. [ ] Substituir `src/index.css` com `@tailwind base/components/utilities` + import de Google Fonts
6. [ ] Adicionar `<link>` do Google Fonts no `index.html`:
   ```html
   <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;800;900&family=Playfair+Display:ital,wght@0,400;0,700;0,800;0,900;1,400;1,700&display=swap" rel="stylesheet">
   ```
7. [ ] Inicializar shadcn:
   ```bash
   npx shadcn@latest init
   # Style: New York | Base color: slate | CSS variables: yes
   ```
8. [ ] Instalar componentes base shadcn:
   ```bash
   npx shadcn@latest add card table button input badge skeleton dialog tooltip select dropdown-menu
   ```
9. [ ] Instalar deps adicionais:
   ```bash
   npm i react-router-dom date-fns date-fns-tz zod bcryptjs recharts
   npm i -D @types/bcryptjs
   ```

### Implementação dos arquivos

10. [ ] `src/theme.ts` — paleta Escala conforme brand-system
11. [ ] `src/lib/types.ts` — TypeScript completo do `DashboardCache` (data-schema seção 4)
12. [ ] `src/lib/formatters.ts`:
    - `formatBRL(value)` → "R$ 1.234,56"
    - `formatPct(value, digits?)` → "12,3%"
    - `formatDuration(seconds)` → "1m 23s" / "2h 15min"
    - `formatDelta(value)` → "↑ 12%" / "↓ 5%"
    - `formatRelativeTime(iso)` → "há 3 minutos"
    - `formatCompactNumber(value)` → "12.3k"
    - `maskPhone(phone)` → "(45) ****-1234"
    - `maskEmail(email)` → "j****@gmail.com"
13. [ ] `src/lib/api.ts` — cliente HTTP único:
    ```typescript
    export async function fetchDashboard(): Promise<DashboardCache> {
      const res = await fetch(import.meta.env.VITE_DASHBOARD_API_URL, {
        headers: { Authorization: `Bearer ${import.meta.env.VITE_DASHBOARD_API_TOKEN}` }
      });
      if (!res.ok) throw new Error(`API error: ${res.status}`);
      const json = await res.json();
      if (!json.ok) throw new Error(json.error);
      return json.data;
    }
    ```
14. [ ] `src/auth/AuthContext.tsx` — React Context com login/logout
15. [ ] `src/auth/LoginPage.tsx` — formulário de senha + bcrypt compare
16. [ ] `src/auth/ProtectedRoute.tsx` — wrapper que redireciona não autenticados
17. [ ] `src/hooks/useDashboardData.ts` — fetch + estado + refresh automático 5min
18. [ ] `src/hooks/useAuth.ts` — consumir AuthContext
19. [ ] `src/components/shared/Card.tsx` — wrapper com identidade Escala
20. [ ] `src/components/shared/DeltaBadge.tsx`
21. [ ] `src/components/shared/EmptyState.tsx`
22. [ ] `src/components/PeriodFilter.tsx`
23. [ ] `src/components/DashboardContainer.tsx` — layout shell com 6 placeholders
24. [ ] `src/App.tsx` — Router + AuthProvider + Routes
25. [ ] `src/main.tsx` — entry point

### Configuração de secrets

26. [ ] Gerar hash bcrypt da senha:
    ```bash
    node -e "console.log(require('bcryptjs').hashSync(process.env.DASHBOARD_PASSWORD_RAW, 10))"
    ```
    Adicionar resultado em `.env.local` como `VITE_DASHBOARD_PASSWORD_HASH`
27. [ ] Configurar prefixos VITE_ no `.env.local`:
    - `VITE_DASHBOARD_API_URL`
    - `VITE_DASHBOARD_API_TOKEN`
    - `VITE_DASHBOARD_PASSWORD_HASH`

### Validação

28. [ ] `npm run dev` — testar localhost:5173
29. [ ] Acessar → ser redirecionado para `/login`
30. [ ] Inserir senha → ver shell com placeholders
31. [ ] Logout funcionando
32. [ ] `npm run build` sem warnings
33. [ ] `npm run lint`
34. [ ] `npx tsc --noEmit`

### Git

35. [ ] Commit inicial: `chore(dashboard): Setup inicial Vite + React + Tailwind (Story 1.3)`
36. [ ] Commit shell: `feat(dashboard): Story 1.3 — Auth Shell + identidade Escala (Epic 001)`
37. [ ] Push para `main`: `git push -u origin main`

---

## File List

```
public/
└── escala-logo.png                  ← baixar do site ou fallback URL

src/
├── App.tsx
├── main.tsx
├── theme.ts
├── index.css
├── auth/
│   ├── AuthContext.tsx
│   ├── LoginPage.tsx
│   └── ProtectedRoute.tsx
├── components/
│   ├── DashboardContainer.tsx
│   ├── PeriodFilter.tsx
│   ├── shared/
│   │   ├── Card.tsx
│   │   ├── DeltaBadge.tsx
│   │   └── EmptyState.tsx
│   └── ui/                           ← shadcn auto-generated
├── hooks/
│   ├── useDashboardData.ts
│   └── useAuth.ts
├── lib/
│   ├── api.ts
│   ├── types.ts
│   ├── formatters.ts
│   └── utils/
│       └── status.ts
└── index.css

index.html                            ← Google Fonts adicionada
tailwind.config.js                    ← paleta Escala
postcss.config.js
package.json
tsconfig.json
vite.config.ts
.env.example
.env.local                            ← já existe (com secrets)
.gitignore                            ← já existe
```

---

## Notes / Dev Hints

- Logo: tentar baixar de `https://escalanegociosdigitais.com.br/logo.png`. Se não disponível, gerar TODO em `docs/decisions/adr-029.md` e pedir ao Cleiton via checkpoint
- **Vite usa prefixo `VITE_`** para expor vars ao browser. Vars sem prefixo ficam só server-side (mas Vite roda build-time, então rigorosamente tudo é "build-time exposed")
- Para shadcn detectar `src/` como base, garantir `tsconfig.json` tem `paths: { "@/*": ["src/*"] }`
- Loading state: usar shadcn `<Skeleton />` em todos os blocos
- Não usar `localStorage` para sessão — preferir `sessionStorage` (perde ao fechar, OK)
- Para o logo na `LoginPage`: 120px de altura com drop-shadow dourado

---

## Definition of Done

- [ ] Todos os ACs marcados
- [ ] Login funcionando local com senha definida
- [ ] Shell renderizando placeholders dos 6 blocos
- [ ] Identidade Escala visualmente correta (validar contra brand-system.md)
- [ ] Build, lint, typecheck passando
- [ ] Commit + push para `main`
- [ ] Vercel preview gerado: `vercel --prebuilt`
